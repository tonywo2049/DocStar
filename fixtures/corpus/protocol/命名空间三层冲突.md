---
目标: fixture 合成命名空间三层冲突（eg-2 隔离级）——覆盖 EG-12-AC5 一条作用域规则替三补丁：同一裸 ID 同时落「文档 frontmatter namespace / E 节语境 / 映射表列语境」三层，最具体优先＝表列 > 节 > 文档兜底（证顺序符合语料语义，非只证既定顺序）
性质: 规范
namespace: 局部命名空间
上游:
  - [A轨需求](A轨-Core与跨链/A轨需求.md)
下游: []
状态: fixture（隔离级）
类型: 结论型
---

# 合成命名空间三层冲突（fixture）

本文 frontmatter 声明 namespace 为「局部命名空间」（文档兜底层）。下节为 E 节语境（D{n}→E-D{n} 节层），且内嵌一张映射矩阵（AC 列＝表列层）。裸 ID D5 同时落三层，按 EG-12-AC5 最具体优先＝表列 > 节 > 文档兜底解析（列胜节、节胜文档）。

## E. 期权与映射矩阵（三层同时存在）

E 节语境（若仅此层，D5 → 治理期权 REQUIREMENTS-E）。但下表是映射矩阵、AC 列为最具体的表列语境（映射表 AC 列 → REQUIREMENTS），故 `D5` 落 AC 列时命名空间取 **REQUIREMENTS**（表列胜过 E 节 REQUIREMENTS-E、胜过文档兜底「局部命名空间」）：

| AC | 内容摘要 | 契约锚 | 归属 |
|---|---|---|---|
| D5 | 三层冲突裸 ID（表列语境最具体 → REQUIREMENTS，非 REQUIREMENTS-E、非 局部命名空间） | C1-AC1 | 冲突证例 |
| R1-AC1 | 常规映射行（对照） | C1-AC1 | A1 |

## E.2 对照：仅 E 节层（无表列覆盖）

散文中裸引 D9（仅 E 节语境、无表列/无更具体层）→ 治理期权 REQUIREMENTS-E（节层胜过文档兜底「局部命名空间」）。
