---
目标: fixture 合成记述文档（eg-2 隔离级）——覆盖 provenance 边（记述→AC/节，固定引用形「本文测/审的是 <X>」）、被测 commit 作边属性非端点、悬空靶→unresolved_reference（记述来源报告不进门禁）、记述性质不进判定域、harvest 不扫记述
性质: 记述
上游:
  - [守恒审计器规格](../契约初稿/守恒审计器规格.md)
下游: []
状态: fixture（隔离级）
类型: 过程型
---

# 合成守恒复算记录（fixture，记述性质）

## 1. 复算范围

本文测的是 AUD-AC1（守恒第一层复算，provenance 边正例：记述→审计AC），被测 commit=abc1234、配置=default（commit/配置作边属性，非端点、不实体化）。

本文审的是 裁定簿 §20.3（provenance 边靶＝节条目，doc-prefixed 可确定解析）。

## 2. 悬空靶（诊断，不进门禁）

本文审的是 裁定簿 §97（该节在裁定簿中不存在＝确定性引用形解析失败＝悬空 → unresolved_reference 诊断；节引用不像被引 AC 那样自动建「无定义块」实体，故真悬空；记述来源分级=报告，不进门禁、不 tainted 他检查）。

## 3. 记述性质说明

本文 provenance 边 consumers={}（投影入图但不进活依赖/覆盖/传导判定，EG-D10）；本文粗体 **复算幂等** 不入 harvest 候选（harvest 仅扫规范文档）。
