---
目标: fixture 合成守恒审计器规格（eg-2 隔离级）——覆盖 审计AC 表格行定义形（首列 `**AUD-AC{n}**`、允许注记）+ AUD-AC10 bold ID 后随注记形
性质: 规范
上游:
  - [契约二](契约二-资产包与否定凭据.md)
下游: []
状态: fixture（隔离级）
类型: 结论型
---

# 合成守恒审计器规格（fixture）

## 4. 审计 AC（表格行形，首列 bold ID + 允许注记）

| ID | 判定式 |
|---|---|
| **AUD-AC1** | WHEN 任一 epoch 边界以 Core 公开数据运行第一层 THEN 对每资产返回 `Δ₁ == 0` 的布尔判定，与在途无关。 |
| **AUD-AC2** | WHEN 以链 i 快照高度全量状态运行第二层 THEN 返回原始差额是否被在途清单 + 已登记 escape 完全解释的判定。 |
| **AUD-AC10**（r3.2 新增，F1 核销规则） | WHEN 包经清算轨/逃生舱在 Core nullifier 簿登记兑付 THEN 该包自 InFlight 核销、该链 NetOut 等额回收（bold ID 后随注记形，定义形描述放宽）。 |
