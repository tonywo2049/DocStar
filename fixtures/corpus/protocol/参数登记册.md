---
目标: fixture 合成参数登记册（eg-2 隔离级）——覆盖 参数 kind 定义处（表行 `| `X_y` | …`，primary 非索引）；约束边 eg-2 已删（DG-20 族），本表偏序仅散文不成边；X_orphan 缺行→无定义块
性质: 规范
上游:
  - [REQUIREMENTS](REQUIREMENTS.md)
下游: []
状态: fixture（隔离级）
类型: 结论型
---

# 合成参数登记册（fixture）

地位：参数定义处（非派生索引）。约束关系列仅散文记偏序，eg-2 不再成边（约束边 EG-D8 删）。

## 1. 时序对齐核心

| 参数 | 语义 | 约束关系（散文） | 定值时机 | 当前状态 |
|---|---|---|---|---|
| `U_unbonding` | 解绑期，以 Core epoch 计 | 三期对齐上界 V_packet ≤ W_evidence ≤ U_unbonding | 契约冻结 | TBD |
| `W_evidence` | 证据有效期 | V_packet ≤ W_evidence ≤ U_unbonding | 契约冻结 | TBD |
| `V_packet` | 跨链凭据有效期 | V_packet ≤ W_evidence | 契约冻结 | TBD |
| `D_avail` | 数据不可得容忍时长 | D_avail < W_evidence 且 D_avail ≤ 3600 | 契约冻结 | TBD |

## 2. 验证者集合与质押

| 参数 | 语义 | 约束关系（散文） | 定值时机 | 当前状态 |
|---|---|---|---|---|
| `P_max_validators` | 每 epoch 活跃集规模上限 | P_max_validators ≥ P_min_validators | D7 | TBD，测试基准值＝21 |
| `P_min_stake` | 验证者准入最低质押量 | P_min_stake ≥ P_min_self_stake | 创世 | TBD |
